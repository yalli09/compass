version: '3.8'

services:
  compass:
    build: .
    container_name: compass-app
    ports:
      - "5030:5030"
    volumes:
      - ./data:/app/data
    environment:
      - FLASK_ENV=production
    restart: unless-stopped
